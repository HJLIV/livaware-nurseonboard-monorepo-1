# Livaware NurseOnboard

Full-stack TypeScript monorepo (Express 5 + React/Vite + PostgreSQL/Drizzle) combining **Clinical-Skills-Arcade**, **Nurse-Preboard**, and **Nurse-Onboard**. Deployed on Replit autoscale.

## Run

- `npm run dev` — Express on :5000 (serves Vite)
- `npm run build` / `npm start` (prod = `node dist/index.cjs`)
- `npm run db:push` — Drizzle schema push
- `npm test` — Vitest API tests (outbound email is suppressed in test mode via `isEmailSendingSuppressed()` in `server/outlook.ts`; `tests/setup.ts` auto-cleans created test nurses/candidates per file)

## Architecture

- **Frontend**: `client/` — React 18, Tailwind, Radix/shadcn, Framer Motion, Recharts, RHF + Zod
- **Backend**: `server/` — Express 5, express-session, MSAL SSO, Microsoft Graph, OpenAI/Anthropic
- **Shared schema**: `shared/schema.ts` — Drizzle + Zod. FK column is `nurseId` everywhere. Compatibility aliases: `candidates → nurses`, `magicLinks → portalLinks`, `users → arcadeUsers`, `modules → arcadeModules`, `assessments → preboardAssessments`.
- **Stage terminology**: DB enum (`preboard | onboard | skills_arcade | completed`) is unchanged; UI shows display names **Applicant / Candidate / Skills Arcade / Nurse** via `STAGE_DISPLAY_NAMES`.

## Modules

- **Nurse-Onboard** (`server/routes/onboard.ts`, `portal.ts`, `referee.ts`) — AI compliance/NMC/DBS/references/certificates/passport/health-triage, Outlook + SharePoint, magic-link portal.
- **Skills Arcade** (`server/routes/skills-arcade.ts`) — 40 seeded scenarios, scoring, trainer remediation, admin reports. Platform admins bridge in via `isPlatformAdmin()`. **Auto-enrolment**: a newly-provisioned nurse arcade account is immediately enrolled in every current module (`enrolUserInAllModules` helper, idempotent skip-if-exists, "not started" status) via `ensureArcadeUserForNurse` (best-effort — never fails account creation; pass `{autoEnrol:false}` when the caller owns counting). Super-admin backfill `POST /api/admin/enrol-all` (default no emails, opt-in `{notify:true}`) iterates all platform nurses, ensures each arcade user, enrols missing modules, returns `{nursesProcessed, enrolmentsCreated, skippedNurseIds, emailsSent}`. **Per-nurse failures are isolated** (each loop iteration is try/caught → logged + pushed to `skippedNurseIds` + `continue`) so one bad record can never 500 the whole backfill; `ensureArcadeUserForNurse` returns `null` (skip) on a missing email and falls back to `getUserByUsername` before `createUser` to dodge the `arcade_users.username` UNIQUE constraint when two nurses share an email. "Enrol All Nurses" button on `/arcade/admin/modules`. Manual "Assign to Nurses" flow unchanged. Audits via existing `skills_arcade`/`assign_module` taxonomy (`detail.auto:true`, `reason: arcade_account_created | enrol_all_backfill | enrol_all_backfill_run`).
- **Preboard** (`server/routes/preboard.ts`) — Timed AI assessment, PDF + email.
- **Onboarding access gate** (`server/services/onboarding-gate.ts`, middleware `requireOnboardingUnlocked`) — Onboarding/Compliance/Arcade locked until 3 prerequisites (preboard examination, ≥1 competency declaration, admin CV-review). Per-nurse `onboardingUnlockMode` (`auto`/`manual`). Locked portal writes return `403 { error: "onboarding_locked", gate }`.
- **Grace early-access** (per-nurse) — `nurses.graceAccessEnabled` (+ `graceAccessUpdatedAt/By`) lets an admin open the **whole Training section** (Mandatory Training, Internal Training, Clinical Skills Arcade), **Invoicing** and **Availability** for an individual nurse *before* compliance approval, induction completion and the `completed` stage are reached. It does **not** bypass the Assessment gate (`requireOnboardingUnlocked` is untouched). Surfaced in `GateState.graceAccess` (`gateStateFromNurse`). Backend bypasses: `requireInductionAcknowledged` (`server/middleware.ts`) short-circuits when the flag is set; invoices `requireCompletedStageRead/Write` and availability `ensureCompletedNurse` allow `currentStage === "completed" || graceAccessEnabled`. Admin toggle: `PUT /api/nurses/:id/grace-access` (`requireAdmin`, body `{enabled}`, idempotent, audit `admin` / `grace_access_granted` | `grace_access_revoked`), returns the gate state. FE: `OnboardingAccessPanel` Switch (`/nurses/:id` → onboarding-access card); `portal-shell.tsx buildPortalGroups` opens Training when `gate.graceAccess`; portal pages (`portal-hub`, `section`, `internal-training`, `invoices`, `availability`) treat `gate.graceAccess === true` like `completed` for Availability/Invoices visibility.
- **Service Agreement** (`server/service-agreement/contract.ts` + `pdf.ts`, `server/routes/service-agreement.ts`) — Livaware's "Service Agreement — Registered Nurse" (task 170) is the **first step of the Induction group** — it is **not** a blocking first step and does **not** lock the rest of the portal. It only becomes signable **once an admin has approved the nurse's compliance** (`gate.complianceApproved`, set via `POST /api/nurses/:id/compliance-approval`). The portal `draft`/`sign` routes enforce this with `requireComplianceApproved` (returns `403 { error: "compliance_not_approved" }` until approved); reading the contract is always allowed. **On-screen typed signature only — no upload.** The nurse reads the full 16-clause contract (read-to-end gate), types their full legal name, and completes identity fields (NMC PIN required, RCN/equivalent membership no. required, UTR/company number required, optional business name). Signing captures name/timestamp/IP/UA and renders a countersigned PDF stored as a `document`. **Reuses the declarations infrastructure — no table change**: `nurseDeclarations` with `declarationKey = "service_agreement"` (`SERVICE_AGREEMENT_KEY` in `contract.ts`); answers hold `{nmcPin, rcnMembershipNo, utrCompanyNumber, businessName}`. **Fully editable + versioned (re-sign on amend)**: the *entire* contract — title, preamble, every clause (text/items, add/remove/reorder), execution note, **Recovery Fee** (clause 12.1 token `{{RECOVERY_FEE}}`) and **countersignatory name/position** — is super-admin editable from `admin-settings.tsx` `ServiceAgreementConfigCard`. The full content lives in `appSettings` key `service_agreement_config` (defaults from `DEFAULT_SERVICE_AGREEMENT_CONFIG` at the **end** of `contract.ts` — defined after the seed constants to avoid a TDZ ReferenceError). **Every content change bumps the version**: `saveServiceAgreementConfig` compares a `contentSignature(...)` of the normalized content and increments `version` on any difference, returning `{config, versionBumped, previousVersion}`; the settings PUT audits `agreement_amended` (with previous/new version) when bumped, else `config_updated`. A version bump forces **all** previously-signed nurses to re-sign: `summarizeAgreement(latest, latestSubmitted, currentVersion)` derives `signed` (latest **submitted** version === current), `everSigned`, and `needsResign` (`everSigned && !signed`) from the latest *submitted* row (NOT `getLatest`, which can be a fresh re-sign draft and would mask the outstanding re-sign — see `getLatestSubmitted` in `declarations/storage.ts`). Portal/admin show a "re-sign required" banner; portal draft/sign create a new `nurseDeclarations` draft at the current version (no spurious 409 — 409 only when the *current* version is already submitted). PDF (`pdf.ts`) renders from the live config. Routes: portal `GET /api/portal/:token/service-agreement` (contract + state), `PUT /api/portal/:token/service-agreement/draft`, `POST /api/portal/:token/service-agreement/sign`; admin `GET /api/nurses/:id/service-agreement` (status + PDF + `currentVersion`/`needsResign`); settings `GET`/`PUT /api/admin/settings/service-agreement` (PUT super-admin, full content). Frontend: portal page `client/src/pages/portal/service-agreement.tsx` (tokenless `/portal/service-agreement` + legacy `/:token`), admin `client/src/components/admin/service-agreement-panel.tsx` (nurse-detail "Service Agreement" tab), `ServiceAgreementConfigCard` in `admin-settings.tsx`. `portal-shell.tsx buildPortalGroups` renders the Service Agreement as the first item of the **Induction** group (`onboard:service-agreement`): locked until `gate.complianceApproved`, then actionable, then "Signed" (flips back to actionable on a version bump). Audit module `service_agreement` (actions `agreement_signed`, `agreement_pdf_generated`, `agreement_viewed`, `agreement_amended`, `config_updated`). Tests: `tests/34-service-agreement.test.ts`.
- **Uniform sizing** (task 153) — `nurses.uniformTopSize / uniformTrouserSize / uniformTrouserLength` (+ `uniformSizingUpdatedAt/By`). Length is enum (`short|regular|long`); top/trouser are free text ≤80 chars. Admin edits via `PUT /api/nurses/:id/uniform-sizing` (audit `admin / uniform_sizing_updated`); nurse self-corrects via `PUT /api/portal/:token/uniform-sizing` (audit `portal / portal_uniform_sizing_updated`). Both surfaces share `applyUniformSizingUpdate` in `server/routes/admin.ts`. CSV pull for the office at `GET /api/admin/uniform-sizing.csv` (button on `/nurses`). One-off WhatsApp backfill via `POST /api/admin/uniform-sizing/backfill` (super-admin, idempotent — covers Roderick Cabbab, Meseret Tsegay, Shafiq Kusi, Ricardo Roxo, Joriza Monforte, Bisrat Haile, Maica Sanca, Naresh Kaur, Ruth Bobiles).
- **Availability** (`server/routes/availability.ts`) — `nurseAvailability(nurseId,date,shift)` unique. UI shows **Day + Night** only via `VISIBLE_SHIFTS = ["am","night"]` (legacy `pm` enum kept for historic rows). Editable window = current month + 6. Gated to `currentStage === "completed"`. Cells support left-click paint or right-click status menu. Portal page shows EWTD + double-shift safety notices. Admin matrix at `/reports/availability` + normalized CSV export.
- **Invoicing** (`server/routes/invoices.ts`) — `invoices` + `invoiceTimesheetEntries` + `invoiceAdditionalCosts`. Money in pence, hours in minutes. Status state machine: `submitted → {approved, rejected}`, `approved → {paid, rejected}`, `paid → {reconciled}`. Gated to completed nurses. Auto PDF + email to `INVOICE_RECIPIENT_EMAIL` on submit (`server/invoice-pdf.ts`, `server/outlook.ts`). Re-download: `/api/portal/:token/invoices/:id/pdf` and `/api/admin/invoices/:id/pdf`.
- **Training chase** (`server/training-notifications.ts`, `server/training-chase-scheduler.ts`) — Outstanding-module computation, editable subject/body templates with tokens (`{{NAME}} {{MODULES_LIST}} {{COUNT}} {{PORTAL_URL}} {{PORTAL_EXPIRY}}`), per-recipient secure 30-day portal link, mailbox auto-ingest of replies (high-confidence → auto-attach; low-confidence → flagged review queue). Scheduled weekly bulk + 30-min reply scan (toggleable via `/settings`).
- **Internal Training** (`server/routes/internal-training.ts`) — Lightweight roster-wide certificate matrix (NO LMS click-through). Fixed catalogue of 4 trainings is a constant in `shared/schema.ts` (`INTERNAL_TRAININGS` / `INTERNAL_TRAINING_KEYS` / `InternalTrainingKey`): `eol_health` (E-learning for Health — End of Life), `livaware_palliative` (Livaware Palliative Care Fundamentals), `world_class_care` (World Class Care Principles), `acute_to_community` (Acute-to-Community Transition). Nurses upload one completion certificate per training (from external platforms) with an optional `completedDate`; storage row `internalTrainingCertificates` is unique on `(nurseId, trainingKey)` so re-upload replaces (onConflictDoUpdate). Files mirror to the object-storage bucket like other uploads. Admin matrix at `GET /api/admin/reports/internal-training-matrix` (green cell = uploaded, `href` to download the cert; grey = not uploaded, excluded from % denominator). Admin upload-on-behalf `POST /api/admin/nurses/:nurseId/internal-training`, delete `DELETE /api/admin/internal-training/:id`, serve `GET /api/admin/internal-training/:id/certificate` (all `requireAdmin`). Nurse self-service under `/api/portal/:token/internal-training` (`validatePortalToken`, GET list + POST upsert + cert serve restricted to own rows) — shares `performUpsert` with the admin side. `trainingKey`/`completedDate` accepted from body **or** query string so the shared `FileUpload` (file-only multipart) works via a parameterized URL. Admin UI: matrix at `/reports/internal-training` (Reports sidebar). Nurse UI: `/portal/internal-training` (Training → "Internal Training" in portal sidebar). Audit module `internal_training` (actions `certificate_uploaded`, `certificate_deleted`).
- **Rostering** (`server/routes/rostering.ts`, task 176) — Patient care packages + nurse allocation. `rosterPatients` (carePattern `twenty_four_hour` = exactly 2 slots, default Day 08:00–20:00 / Night 20:00–08:00; `timed_visits` = 1–3 per day; slot times editable per patient; optional engagement window) and `rosterAllocations` (unique on patientId+date+slotKey, snapshots slotLabel/start/end; upsert replaces the cell). Crossover (double-booking) detection uses absolute-minute intervals across date±1 so midnight-crossing night shifts conflict with next-morning visits; `POST .../allocations` returns `409 {error:"crossover_conflict",conflicts}` unless `override:true` (audited `detail.overridden`). Nurse picker `GET /api/admin/rostering/nurse-options?date&startTime&endTime` returns compliance quick-ticks (NMC/DBS `verified`, ≥1 `approved` competency declaration), availability hint (slot window mapped to am/pm/night) and existing conflicts. Consolidated `GET /api/admin/rostering/rota` (staff totals shifts/hours, per-patient coverage/gaps, conflicts) + `GET .../conflicts`. All admin endpoints `requireAdmin` under `/api/admin/rostering/*`. Portal read-only "My Shifts" `GET /api/portal/:token/my-shifts` (gated `completed || graceAccessEnabled`; month-grouped with hour totals; exposes patient **first name only**). Audit module `rostering` (`patient_created/updated/deleted`, `nurse_allocated`, `nurse_unallocated`). Admin pages `/rostering/patients`, `/rostering/patients/:id` (grid), `/rostering/rota` (Rostering sidebar group, adminOnly); portal page `/portal/my-shifts` — reached via the **Roster** rail tab, which lands on My Availability; both roster pages show a `RosterTabs` pill switcher (`client/src/components/portal/roster-tabs.tsx`) toggling My Availability ↔ My Shifts (the sidebar group items aren't rendered as a dropdown). Tests: `tests/35-rostering.test.ts`.
- **Semble sync** (`server/semble/client.ts` + `service.ts`, `server/routes/semble.ts`) — Practice-management integration with Semble (GraphQL, `x-token` header, `SEMBLE_API_TOKEN` + optional `SEMBLE_API_URL`). Admins link roster patients to Semble patients from the patient dialog on `/rostering/patients` (search ≥2 chars via `GET /api/admin/semble/patients?search=`, pick → prefills name/dob/email/phone + `rosterPatients.sembleId`, unlink = null; `sembleId` unique-indexed). **Booking push is confirm-and-push** (NOT automatic): allocations stay local ("pending") until an admin reviews and pushes them in bulk. **Push is currently feature-flagged OFF** while rostering beds in: `SEMBLE_PUSH_ENABLED = false` in `shared/schema.ts` greys out the "Push to Semble" buttons (rota + patient roster) and makes the push endpoint return `503 semble_push_disabled`; flip the constant to `true` to re-enable (no other changes needed — cancellation and patient/nurse linking stay live). `GET /api/admin/semble/pending-bookings?from&to[&patientId]` (admin) lists allocations for linked active patients with per-row status `pending | failed | booked` + counts; `POST /api/admin/semble/push-bookings` `{allocationIds:1..200}` (admin) awaits `pushAllocationBooking` sequentially, returns per-row results `{status: created|failed|skipped}` + summary, 503 when unconfigured / 409 `semble_settings_incomplete` before any work, audits summary `booking_push_run`. FE: `SemblePushDialog` (`client/src/components/rostering/semble-push-dialog.tsx`) — checkbox list (pending+failed pre-ticked), confirm button, per-shift result badges — opened from "Push to Semble" buttons on `/rostering/rota` and `/rostering/patients/:id` (button only when patient linked); grid cells on the patient roster show a status dot (green booked / red failed / amber pending) for linked patients. **Cancellation stays automatic + fire-and-forget**: unassigning cancels the booking; replacing a booked cell with a different nurse/times cancels the stale booking and resets the row to pending (`cellChanged` logic in the allocation POST clears `sembleBookingId/Error`; re-assigning the identical nurse keeps the booking). **Nurse ↔ Semble account linking**: `nurses.sembleUserId/sembleUserName` map a platform nurse to their own Semble user account so pushed bookings land in that nurse's own Semble diary (`pushAllocationBooking` uses `nurse.sembleUserId || settings.doctorId`; audit detail records `sembleDoctorId` + `bookedAsNurse`). Unlinked nurses fall back to the super-admin-configured defaults (location / booking type / default clinician — nurse named in booking comments). Routes: `GET /api/admin/semble/users` (live active Semble user list), `PUT /api/admin/semble/nurse-link/:nurseId` (admin, `{sembleUserId, sembleUserName}` or null to unlink; linking steals the Semble user from any other nurse holding it — one account ↔ one nurse; audits `nurse_linked`/`nurse_unlinked`), `POST /api/admin/semble/auto-match-nurses` (admin, email-first then exact-full-name match, ambiguous duplicates dropped, never overwrites existing links, audits per-link + `nurse_auto_match_run`). FE: "Nurse account matching" section in `SembleSyncCard` on `/settings` (Semble user list with per-user nurse Select + Auto-match button; status card shows Linked nurses count); `SemblePushDialog` rows show "books as {name}" vs "books as default clinician" (`nurseLinked`/`bookedAsName` on pending-bookings items). Local roster writes NEVER fail on Semble errors — failures land on `rosterAllocations.sembleBookingError` + audit. Tests never touch the live API: `tests/setup.ts` deletes `SEMBLE_API_TOKEN` and `36-semble.test.ts` snapshots/restores the booking-defaults setting. Wall-clock slot times convert via `zonedIso` (Europe/London default, handles midnight-crossing nights). Booking defaults live in `appSettings` key `semble_booking_config`, edited from the **Semble practice-management sync** card on `/settings` (`SembleSyncCard`; save + test-connection are super-admin). Routes under `/api/admin/semble/*`: status (admin), patients + booking-options (admin), test-connection + settings PUT (super-admin); errors degrade to `503 semble_not_configured` / `502 semble_api_error`. No portal surface by design. Audit module `semble` (`patient_linked`, `patient_unlinked`, `booking_created`, `booking_failed`, `booking_cancelled`, `booking_cancel_failed`, `booking_push_run`, `config_updated`). Tests: `tests/36-semble.test.ts`.
- **Policies** (`server/routes/policies.ts`) — Admin CRUD; bumping version forces re-acknowledgement. Nurse page: `/portal/policies/:token`.
- **Super-admin Activity Dashboard** (`server/routes/super-admin.ts`) — Live audit feed + per-actor leaderboard at `/super-admin/activity`.
- **Email templates** (`server/email-templates.ts`, `server/routes/announcements.ts`) — **Single source of truth** for every transactional email the platform sends (currently 14 templates: applicant welcome, onboarding unlocked, outstanding-items nudge, sign-in reminder, portal sign-in code, invoice submitted, referee request reminder, referee thank-you, training chase, document-upload notification, platform update announcement, launch announcement, plus internal variants). Each template is registered with: stable `key`, label, description, category (`nurse | internal | broadcast`), branded HTML envelope (header/footer), token list, default subject, and an array of **per-section plain-text `fields`** (each with `name`, `label`, `kind: text | textarea | list`, default, optional help/rows). The renderer (`renderEmail(key, tokens)`) resolves the persisted subject + fields (falling back to built-in defaults per field), applies mustache-lite tokens (including conditional `{{#X}}…{{/X}}` blocks), and assembles the result into the gold/navy NurseOnboard HTML shell — admins never touch markup. All eleven Outlook senders in `server/outlook.ts` are thin shims around `sendViaTemplate({key, tokens, to})`. Persistence: `emailTemplates` table (`key, subject, bodyHtml, bodyText, fields jsonb, updatedAt, updatedBy`); legacy `bodyHtml/bodyText` columns are kept notNull (written as `""`) — the canonical content now lives in the `fields` JSON. Super Admins edit every template from `/settings` via the **Editable email templates** card (`AllEmailTemplatesCard` + per-template `EmailTemplateEditor`), grouped by category, with per-field text/textarea/list inputs, token cheatsheet, in-dialog HTML preview, and "Reset to default" per field. Routes (all super-admin gated): `GET /api/admin/email-templates` (list with current+defaults), `GET /api/admin/email-templates/:key`, `PUT /api/admin/email-templates/:key` (body `{subject, fields}`), `GET /api/admin/email-templates/:key/preview`.
- **Supervision & Appraisals** (`server/routes/supervisions.ts`, task 162) — Admin-only per-nurse log of 1:1 supervision calls, appraisals, and reflective conversations. Stored on `nurseSupervisions` (type enum `supervision|appraisal|reflection|other`, `conversationDate` text `YYYY-MM-DD`, required `notes`, optional file attachment held on the row — never in `documents` so the portal can't enumerate it). All endpoints sit under `/api/admin/*` (inherits `requireAdmin`). PUT/DELETE require row author **or** super-admin. Surfaced on `/nurses/:id` via the "Supervision & Appraisals" tab (timeline + add/edit Dialog with file picker, notes shown `whitespace-pre-wrap`). Attachments served via `/api/admin/supervisions/:id/attachment` (admin-only). Audit module `supervision` with actions `supervision_added | supervision_updated | supervision_deleted`. No portal route exists for this surface by design — the nurse cannot see these entries.
- **HBC training sync** (`server/hbc/client.ts`, `server/hbc/service.ts`, `server/routes/hbc.ts`) — Integration with the **Healthier Business Group** compliance/training portal (https://dev.hbcompliance.co.uk) to sync mandatory CSTF training. Auth uses HB's V2 bearer-token flow (`POST /auth/token` with `{client_id, api_key}` → access token cached in-memory, lazy-refreshed 60s before expiry, auto re-auth on 401); candidate/training/certificate data read & written via HB's resource endpoints, which still also gate on `API-Key`/`API-Version` headers (sent alongside the bearer token). Full sync: pull course catalogue (`GET /training-list` → `hbcCourses`), push/create candidates (`POST /candidates`, form-urlencoded → `hbcCandidateLinks`, idempotent — 202 = already existed), pull per-nurse results (`GET /training-results/{ref}` → `hbcTrainingResults`, replace-on-sync), live candidate fetch, and combined certificates PDF download. Three admin-only mirror tables (`hbcCourses`, `hbcCandidateLinks` one-per-nurse, `hbcTrainingResults` unique on nurse+course) — **no portal surface by design**. Config via env (`HBC_CLIENT_ID`, `HBC_API_KEY`, `HBC_API_BASE_URL`, optional `HBC_API_VERSION`); `isHbcConfigured()` gates everything and routes return `503 hbc_not_configured` / `502 hbc_api_error` when HB is unreachable. Routes under `/api/admin/hbc/*` (status + reads = admin; test-connection, catalogue sync, candidate push, report sync = **super-admin**). Audit module `hbc` (actions `course_catalog_synced`, `candidate_created`/`candidate_matched`, `training_report_synced`). Super Admins drive it from the **HBC training sync** card on `/settings`.
- **Announcements** (`server/announcements.ts`, `server/routes/announcements.ts`) — Two one-off broadcast emails the Super Admin can fire from `/settings`: the **Platform update announcement** (heads-up that Skills Arcade / Policies / Availability / Invoicing are all live) and the **Platform launch & invoicing announcement** (introduces the portal, embeds the explainer video link when `PLATFORM_VIDEO_URL` is set, hard CTA to `https://onboard.livaware.co.uk`, informs nurses that all invoices must be submitted via the portal from 1 June 2026 because the legacy invoicing system is deprecated end of May 2026). Subject + HTML + plain-text body are stored in the **`emailTemplates`** table (key, subject, bodyHtml, bodyText, updatedBy/At) and seeded on first read. SAs edit them in `/settings` via `EmailTemplateEditor` (PUT `/api/admin/email-templates/:key`, super-admin only); preview/send always render through `renderAnnouncement(key, tokens)` with mustache-lite tokens (`{{NAME}}`, `{{PORTAL_URL}}`, `{{VIDEO_URL}}`) and conditional `{{#VIDEO_URL}}…{{/VIDEO_URL}}` blocks. Both share the same recipient filter (`!!email && !!fullName && currentStage !== "withdrawn"`), Microsoft Graph sender, per-recipient try/catch, and write per-recipient + summary audit rows under module `announcements` (actions `platform_update_announcement_sent`, `launch_announcement_sent`, `launch_announcement_run`, `email_template_updated`).

## Frontend Pages

**Admin core**: `/` (dashboard), `/candidates`, `/candidates/:id`, `/pipeline`, `/nurses`, `/nurses/:id`, `/preboard`, `/audit`, `/guide`, `/settings`.
**Reports**: `/reports/onboarding`, `/reports/training`, `/reports/competency`, `/reports/declarations`, `/reports/availability`, `/reports/invoices`, `/admin/policies`.
**Rostering (admin only)**: `/rostering/patients`, `/rostering/patients/:id`, `/rostering/rota`.
**Skills Arcade**: `/arcade`, `/arcade/scenario/:id`, `/arcade/walkthrough/:id`, `/arcade/trainer`, `/arcade/admin/{modules,reports,users}`.
**Portal (token-gated)**: `/portal/:token` (hub), `/portal/page/:token`, `/portal/service-agreement` (first, blocking step), `/portal/availability`, `/portal/my-shifts`, `/portal/invoices`, `/portal/policies/:token`, `/preboard/assessment`, `/referee/:token`.
**Super-admin**: `/super-admin/activity`.

Sidebar groups (admin) live in `client/src/components/layout/sidebar-nav.tsx`. Portal sidebar groups live in `portal-shell.tsx` `buildPortalGroups` and are stage-gated (e.g. Availability + Invoices visible only when `currentStage === "completed"`).

## Authentication

- **Local**: `ADMIN_USERNAME`/`ADMIN_PASSWORD`, `TEAM_USERNAME`/`TEAM_PASSWORD`, optional `SUPER_ADMIN_USERNAME`/`SUPER_ADMIN_PASSWORD` (checked first).
- **Microsoft 365 SSO** via MSAL (`server/msal-auth.ts`). Requires `AZURE_AD_TENANT_ID`, `AZURE_AD_CLIENT_ID`, `AZURE_AD_CLIENT_SECRET`. Redirect: `https://<domain>/api/auth/microsoft/callback`. If `SUPER_ADMIN_EMAIL` matches the SSO email, session is auto-promoted to `super_admin`.
- **Roles**: `admin` (read all, write candidates/nurses/portal/onboard), `team` (limited), `super_admin` (exclusive write to policies, settings, arcade imports, training-chase send/scan, nurse invites). Backend gate: `requireSuperAdmin` middleware. Frontend uses `<SuperAdminGate>` + `<SuperAdminViewOnlyBanner>`.
- **Nurse portal sign-in**: `/api/portal/auth/verify-code` returns a per-nurse `portalUrl` (resolved server-side from freshest non-expired token, or freshly-minted 30-day hub token). `/api/portal/auth/portal-url` re-resolves on every relevant client touchpoint.

## Environment Variables

- `DATABASE_URL`, `SESSION_SECRET`
- `AZURE_AD_TENANT_ID`, `AZURE_AD_CLIENT_ID`, `AZURE_AD_CLIENT_SECRET`, `AZURE_AD_SENDER_EMAIL`
- `SUPER_ADMIN_USERNAME` / `SUPER_ADMIN_PASSWORD` / `SUPER_ADMIN_EMAIL`
- `OPENAI_API_KEY` (or `AI_INTEGRATIONS_OPENAI_API_KEY`), `ANTHROPIC_API_KEY`
- `REPORT_EMAIL` (preboard reports), `INVOICE_RECIPIENT_EMAIL` (default `invoices@livaware.co.uk`)
- `PLATFORM_VIDEO_URL` (optional — explainer video URL embedded in the launch announcement; block hidden when unset), `PORTAL_PUBLIC_URL` (optional, defaults to `https://onboard.livaware.co.uk`)
- `NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN`, `NEXT_PUBLIC_POSTHOG_HOST` (optional — PostHog analytics + session recording; public publishable token. `VITE_*`/`POSTHOG_*` names also accepted. Server injects them into the SPA shell as `window.__POSTHOG__` via `injectPosthogConfig` in `server/posthog-config.ts`; client inits via `PostHogProvider` (`@posthog/react`) in `client/src/main.tsx`. Feature is a no-op when the token is unset.)
- `ADMIN_USERNAME`, `ADMIN_PASSWORD`, `TEAM_USERNAME`, `TEAM_PASSWORD`
- `HBC_CLIENT_ID`, `HBC_API_KEY`, `HBC_API_BASE_URL`, `HBC_API_VERSION` (optional, defaults `1.0`) — Healthier Business Group training-sync integration; all unset = feature shows "not configured"
- `SEMBLE_API_TOKEN` (Semble practice-management sync; unset = feature shows "not configured"), `SEMBLE_API_URL` (optional, defaults `https://open.semble.io/graphql`)

## Design System

- **Colors**: Gold `#C8A96E` accent, deep navy dark theme, warm parchment light theme.
- **Fonts**: Fraunces (serif headings), Be Vietnam Pro (sans body).
- **Page header**: `text-[11px] uppercase tracking-[0.18em] text-muted-foreground/60` eyebrow + `font-serif text-2xl/3xl font-light tracking-tight` heading.
- **Stat numbers**: `font-serif text-2xl font-light` (never bold).
- **Animations**: Staggered fade-in-up with `animate-delay-*`; respects `prefers-reduced-motion`.
- **Tokens**: `client/src/lib/design-system.ts` (BRAND, FONT, DOMAIN_COLORS, GRADIENTS, GRAIN_TEXTURE).
- **Shared components**: `client/src/components/shared/` (`step-progress`, `status-badge`, `file-upload`).

## Audit

All state-changing routes log to `audit_logs` via `storage.createAuditLog({ nurseId, module, action, agentName, detail })`. Module enum (`auditModuleEnum`) and the Super-Admin Activity Dashboard expect a stable taxonomy — see the `audit-trail-conventions` skill.

## Deployment

- Target `autoscale`, build `npm run build`, run `node dist/index.cjs`.

## Project-Specific Agent Skills

Conventions live under `.agents/skills/`. Read the relevant `SKILL.md` before changing that area:

- `portal-admin-parity` — keep portal/admin write surfaces in lockstep
- `sidebar-registry` — admin (`sidebar-nav.tsx`) + portal (`portal-shell.tsx`) nav, role/stage gating
- `schema-cascade` — required order for `shared/schema.ts` changes (alias → FK → audit enum → `db:push` → storage → routes → Zod → FE → tests)
- `audit-trail-conventions` — module/action taxonomy
- `stage-and-role-gating` — middleware picker (`requireAdmin`, `requireSuperAdmin`, `validatePortalToken`, `requireOnboardingUnlocked`, `requireNurseStageCompleted`, `requireInductionAcknowledged`)
- `new-portal-feature`, `new-admin-report`, `chase-and-notification-patterns`
- `ai-service-conventions` — env precedence, fire-and-forget, `documents.aiStatus` taxonomy
- `microsoft-graph-integration`, `vitest-api-tests`, `pre-merge-checklist`, `replit-md-maintenance`
